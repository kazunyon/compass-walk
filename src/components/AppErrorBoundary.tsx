import{Component,type ErrorInfo,type ReactNode}from'react';type Props={children:ReactNode};type State={hasError:boolean};export class AppErrorBoundary extends Component<Props,State>{state:State={hasError:false};static getDerivedStateFromError():State{return{hasError:true}}componentDidCatch(error:Error,info:ErrorInfo){console.error('Application rendering error',error,info)}render(){if(this.state.hasError)return <main className="app-error" role="alert"><h1>画面を表示できませんでした</h1><p>もう一度読み込むと、保存済みの記録を確認できます。</p><button type="button" onClick={()=>window.location.reload()}>再読み込み</button></main>;return this.props.children}}
