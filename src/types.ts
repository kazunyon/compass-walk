export type VisitType='regular'|'makeup'|'absence'|'cancelled'; export type Weather='sunny'|'cloudy'|'rainy'|'snowy'|'unset'; export type StaffRole='理学療法士'|'柔道整復師'|'看護師'|'その他';
export interface Staff{id?:number;name:string;role:StaffRole;createdAt:string} export interface Schedule{id?:number;date:string;type:VisitType;note?:string;updatedAt:string} export interface DailyRecord{id?:number;date:string;weather:Weather;condition?:string;vitals?:{bloodPressure?:string;pulse?:number;temperature?:number};exercises?:string[];staffIds?:number[];lifeChanges?:string[];note?:string;updatedAt:string} export interface Draft{id:'record';data:Partial<DailyRecord>;updatedAt:string}
