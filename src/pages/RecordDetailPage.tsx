import { ArrowLeft, CalendarDays, CheckCircle2 } from 'lucide-react';
import { useLiveQuery } from 'dexie-react-hooks';
import { Link, useParams } from 'react-router-dom';
import db from '../db';
const labels: Record<string,string>={beforeCondition:'利用前の体調',sleep:'睡眠',fatigue:'疲労',mood:'気分',painLevel:'痛みレベル',exerciseMinutes:'運動時間',assistanceLevel:'介助レベル',afterFatigue:'利用後の疲れ',afterPain:'利用後の痛み',satisfaction:'満足度'};
export function RecordDetailPage(){const{id}=useParams();const record=useLiveQuery(()=>db.records.get(Number(id)),[id]);const staff=useLiveQuery(()=>db.staff.toArray(),[]);if(record===undefined)return <p>読み込み中です。</p>;if(!record)return <section className="empty-state"><CalendarDays/><h1>記録が見つかりません</h1><Link className="primary" to="/record">記録を入力する</Link></section>;const names=(record.staffIds??[]).map(x=>staff?.find(s=>s.id===x)?.name).filter(Boolean).join('、')||'未記録';const rows=Object.entries(labels).filter(([k])=>record[k as keyof typeof record]).map(([k,label])=>({label,value:String(record[k as keyof typeof record])}));const lists=[['痛い場所',record.painAreas?.join('、')],['担当者',names],['実施運動',record.exercises?.join('、')],['使用した補助具',record.assistiveDevices?.join('、')],['自宅で行う運動',record.homeExercises?.join('、')]].filter(x=>x[1]);return <><Link className="back-link" to="/review"><ArrowLeft size={17}/>振り返りへ</Link><section className="detail-hero"><small>RECORD DETAIL</small><h1>{new Date(record.date+'T00:00:00').toLocaleDateString('ja-JP',{year:'numeric',month:'long',day:'numeric',weekday:'short'})}の記録</h1><p><CheckCircle2 size={16}/>保存済み</p></section><section className="detail-card"><h2>コンディション</h2>{rows.slice(0,5).map(x=><div className="detail-row" key={x.label}><span>{x.label}</span><b>{x.value}</b></div>)}</section><section className="detail-card"><h2>バイタル</h2><div className="vital-summary"><div><span>体温</span><b>{record.vitals?.temperature?`${record.vitals.temperature} ℃`:'—'}</b></div><div><span>血圧</span><b>{record.vitals?.bloodPressure||'—'}</b></div><div><span>脈拍</span><b>{record.vitals?.pulse?`${record.vitals.pulse} 回/分`:'—'}</b></div><div><span>SpO2</span><b>{record.vitals?.spo2?`${record.vitals.spo2} %`:'—'}</b></div></div></section><section className="detail-card"><h2>運動・支援</h2>{lists.map(x=><div className="detail-row" key={x[0]}><span>{x[0]}</span><b>{x[1]}</b></div>)}{rows.slice(5).map(x=><div className="detail-row" key={x.label}><span>{x.label}</span><b>{x.value}</b></div>)}</section><section className="detail-card"><h2>メモ</h2><div className="memo-row"><span>今日の成果</span><b>{record.achievement||'未記録'}</b></div><div className="memo-row"><span>指導されたこと</span><b>{record.instructions||'未記録'}</b></div></section></>}
