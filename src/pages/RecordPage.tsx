import { useEffect, useRef, useState } from 'react';
import { useForm } from 'react-hook-form';
import { useLiveQuery } from 'dexie-react-hooks';
import { Check, CloudUpload, RefreshCw, Save } from 'lucide-react';
import db from '../db';
import type { DailyRecord, Weather } from '../types';
import { weatherData, weatherIcons, weatherLabels, weatherValue } from '../features/weather/weatherTypes';
import { getWeatherForDate } from '../features/weather/weatherService';
type FormValues = Partial<DailyRecord> & { date: string };
const choices={beforeCondition:['とても良い','良い','ふつう','あまり良くない','悪い'],sleep:['よく眠れた','まあ眠れた','眠りが浅い','あまり眠れなかった'],fatigue:['なし','少しある','ある','強い'],mood:['とても良い','良い','ふつう','沈みがち'],painLevel:['なし','軽い','中くらい','強い'],exerciseMinutes:['10分未満','10〜20分','20〜30分','30分以上'],assistanceLevel:['見守り','一部介助','中等度介助','全介助'],afterFatigue:['なし','少しある','ある','強い'],afterPain:['なし','軽い','中くらい','強い'],satisfaction:['とても満足','満足','ふつう','不満']};
const defaults:FormValues={date:new Date().toISOString().slice(0,10),painAreas:[],staffIds:[],exercises:[],assistiveDevices:[],homeExercises:[]};const multiOptions={painAreas:['なし','首・肩','腰','ひざ','足','その他'],exercises:['ストレッチ','歩行練習','筋力トレーニング','バランス運動','自転車'],assistiveDevices:['なし','杖','歩行器','装具','車いす'],homeExercises:['ストレッチ','散歩','筋力トレーニング','バランス練習']};
export function RecordPage(){const draft=useLiveQuery(()=>db.drafts.get('record'),[]);const staff=useLiveQuery(()=>db.staff.toArray(),[]);const location=useLiveQuery(()=>db.weatherLocations.get('default'),[]);const{register,watch,reset,setValue,getValues,handleSubmit}=useForm<FormValues>({defaultValues:defaults});const[saveMessage,setSaveMessage]=useState('下書きは自動保存されます');const[weatherMessage,setWeatherMessage]=useState('');const[weatherBusy,setWeatherBusy]=useState(false);const initialized=useRef(false);const formValues=watch();const selectedDate=watch('date');const cachedWeather=useLiveQuery(()=>selectedDate?db.weatherCache.where('[locationId+date]').equals(['default',selectedDate]).first():undefined,[selectedDate]);
useEffect(()=>{if(!initialized.current&&draft){reset({...defaults,...draft.data});initialized.current=true}if(!draft)initialized.current=true},[draft,reset]);useEffect(()=>{if(!cachedWeather||weatherData(getValues('weather'))?.source==='manual')return;setValue('weather',{...cachedWeather,source:'auto'},{shouldDirty:true})},[cachedWeather,getValues,setValue]);useEffect(()=>{if(!selectedDate||!location||cachedWeather)return;void getWeatherForDate(location,selectedDate).catch(()=>setWeatherMessage('天気を取得できませんでした。記録はそのまま入力できます。'))},[cachedWeather,location,selectedDate]);const saveDraft=async()=>{await db.drafts.put({id:'record',data:getValues(),updatedAt:new Date().toISOString()});setSaveMessage('下書きを保存しました')};useEffect(()=>{if(!initialized.current)return;const id=window.setTimeout(()=>void saveDraft(),0);return()=>window.clearTimeout(id)},[formValues]);useEffect(()=>{const id=window.setInterval(()=>void saveDraft(),30000);return()=>window.clearInterval(id)},[]);
const refreshWeather=async()=>{if(!selectedDate||!location){setWeatherMessage('利用場所を設定すると天気を取得できます。');return}setWeatherBusy(true);try{const next=await getWeatherForDate(location,selectedDate,true);if(weatherData(getValues('weather'))?.source!=='manual')setValue('weather',{...next,source:'auto'},{shouldDirty:true});setWeatherMessage(weatherData(getValues('weather'))?.source==='manual'?'天気を更新しました。手動変更した天気は保持しています。':'天気を更新しました。')}catch{setWeatherMessage('天気を取得できませんでした。記録はそのまま入力できます。')}finally{setWeatherBusy(false)}};const toggle=(field:keyof typeof multiOptions|'staffIds',value:string|number)=>{const current=((getValues(field)??[])as(string|number)[]);setValue(field,(current.includes(value)?current.filter(x=>x!==value):[...current,value])as never,{shouldDirty:true})};const selectOne=(label:string,field:keyof typeof choices)=><section className="record-section"><h2>{label}</h2><div className="option-grid">{choices[field].map(item=><label key={item} className={(formValues as unknown as Record<string,string>)[field]===item?'option selected':'option'}><input type="radio" value={item}{...register(field)}/>{item}</label>)}</div></section>;const selectMany=(label:string,field:keyof typeof multiOptions|'staffIds',options:{label:string;value:string|number}[])=>{const selected=((formValues as Record<string,unknown>)[field]??[])as(string|number)[];return <section className="record-section"><h2>{label}</h2><div className="option-grid">{options.map(({label:item,value})=><button type="button" key={String(value)} className={selected.includes(value)?'option selected':'option'} onClick={()=>toggle(field,value)}>{selected.includes(value)&&<Check size={15}/>} {item}</button>)}</div></section>};const saveRecord=async(data:FormValues)=>{await db.records.put({...data,weather:data.weather??'unset',updatedAt:new Date().toISOString()}as DailyRecord);await db.drafts.delete('record');setSaveMessage('記録を保存しました')};const currentWeather=weatherValue(formValues.weather);
return <form className="record-form" onSubmit={handleSubmit(saveRecord)}><small>DAILY RECORD</small><h1>今日の記録</h1><p className="draft-status"><CloudUpload size={16}/>{saveMessage}</p><section className="record-section date-section"><label>利用日<input type="date" {...register('date')}/></label></section><section className="record-section"><h2>天気</h2><p className="hint">{weatherData(formValues.weather)?.source==='manual'?'手動で変更済み':cachedWeather?'自動取得した天気':'天気は未取得です'}</p><div className="weather-choices">{(['sunny','cloudy','rainy','snowy','unset']as Weather[]).map(x=><button type="button" className={currentWeather===x?'selected':''} onClick={()=>setValue('weather',{value:x,source:'manual'},{shouldDirty:true})} key={x}><span>{weatherIcons[x]}</span>{weatherLabels[x]}</button>)}</div>{weatherData(formValues.weather)&&<p className="weather-detail">最高：{weatherData(formValues.weather)?.temperatureMax??'—'}℃　最低：{weatherData(formValues.weather)?.temperatureMin??'—'}℃　降水確率：{weatherData(formValues.weather)?.precipitationProbability??'—'}％</p>}<button className="today" type="button" onClick={()=>void refreshWeather()} disabled={weatherBusy}><RefreshCw size={18}/>{weatherBusy?'取得中…':'天気を更新'}</button>{weatherMessage&&<p className="weather-message" role="status">{weatherMessage}</p>}</section>{selectOne('利用前の体調','beforeCondition')}{selectOne('睡眠','sleep')}{selectOne('疲労','fatigue')}{selectOne('気分','mood')}{selectOne('痛みレベル','painLevel')}{selectMany('痛い場所','painAreas',multiOptions.painAreas.map(x=>({label:x,value:x})))}<section className="record-section"><h2>バイタル</h2><div className="vitals"><label>体温<input inputMode="decimal" placeholder="例 36.5 ℃" {...register('vitals.temperature',{valueAsNumber:true})}/></label><label>血圧<input placeholder="例 120 / 80" {...register('vitals.bloodPressure')}/></label><label>脈拍<input inputMode="numeric" placeholder="例 72 回/分" {...register('vitals.pulse',{valueAsNumber:true})}/></label><label>SpO2<input inputMode="numeric" placeholder="例 98 %" {...register('vitals.spo2',{valueAsNumber:true})}/></label></div></section>{selectMany('担当者','staffIds',(staff??[]).map(x=>({label:`${x.name}（${x.role}）`,value:x.id!})))}{selectMany('実施運動','exercises',multiOptions.exercises.map(x=>({label:x,value:x})))}{selectOne('運動時間','exerciseMinutes')}{selectMany('使用した補助具','assistiveDevices',multiOptions.assistiveDevices.map(x=>({label:x,value:x})))}{selectOne('介助レベル','assistanceLevel')}<section className="record-section"><h2>今日の成果</h2><input placeholder="例 休まずに10分歩けた" {...register('achievement')}/></section><section className="record-section"><h2>指導されたこと</h2><input placeholder="例 姿勢を意識する" {...register('instructions')}/></section>{selectMany('自宅で行う運動','homeExercises',multiOptions.homeExercises.map(x=>({label:x,value:x})))}{selectOne('利用後の疲れ','afterFatigue')}{selectOne('利用後の痛み','afterPain')}{selectOne('満足度','satisfaction')}<button className="save record-save" type="submit"><Save size={19}/>記録を保存する</button></form>}